/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AutoFormatDate.directive.tsDirective } from './auto-format-date.directive.ts.directive';

describe('Directive: AutoFormatDate.directive.ts', () => {
  it('should create an instance', () => {
    const directive = new AutoFormatDate.directive.tsDirective();
    expect(directive).toBeTruthy();
  });
});
